<Window x:Class="SoundFXStudio.Views.Dialogs.AssignSoundToKeyDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:SoundFXStudio.Controls"
        Title="Assign Sound"
        Width="600"
        Height="720"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        Icon="/Assets/SoundFXStudioLogo.png">

    <Window.Resources>

        <SolidColorBrush x:Key="PanelBrush" Color="#C1141E2D"/>
        <SolidColorBrush x:Key="TextBrush" Color="#EAF1FF"/>
        <SolidColorBrush x:Key="MutedBrush" Color="#9AAFD0"/>

        <Style x:Key="GlassPanel"
               TargetType="Border">

            <Setter Property="CornerRadius"
                    Value="24"/>

            <Setter Property="Background"
                    Value="{StaticResource SurfaceRaisedBrush}"/>

            <Setter Property="BorderBrush"
                    Value="{StaticResource BorderAccentBrush}"/>

            <Setter Property="BorderThickness"
                    Value="1"/>

        </Style>

        <Style x:Key="GlassButton"
               TargetType="Button">

            <Setter Property="Foreground"
                    Value="White"/>

            <Setter Property="Background"
                    Value="#1F13233A"/>

            <Setter Property="BorderBrush"
                    Value="#3300D4FF"/>

            <Setter Property="BorderThickness"
                    Value="1"/>

            <Setter Property="Cursor"
                    Value="Hand"/>

            <Setter Property="Height"
                    Value="42"/>

            <Setter Property="Template">

                <Setter.Value>

                    <ControlTemplate TargetType="Button">

                        <Border x:Name="ButtonBorder"
                                CornerRadius="14"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">

                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>

                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsMouseOver"
                                     Value="True">

                                <Setter TargetName="ButtonBorder"
                                        Property="BorderBrush"
                                        Value="#00D4FF"/>

                                <Setter TargetName="ButtonBorder"
                                        Property="Background"
                                        Value="#2A00D4FF"/>

                            </Trigger>

                            <Trigger Property="IsPressed"
                                     Value="True">

                                <Setter TargetName="ButtonBorder"
                                        Property="Background"
                                        Value="#3300D4FF"/>

                            </Trigger>

                        </ControlTemplate.Triggers>

                    </ControlTemplate>

                </Setter.Value>

            </Setter>

        </Style>

    </Window.Resources>

    <Border Style="{StaticResource AppWindowRoot}"
            CornerRadius="28"
            BorderThickness="1.5">

        <Grid Margin="18">

            <Grid.RowDefinitions>

                <RowDefinition Height="75"/>

                <RowDefinition Height="90"/>

                <RowDefinition Height="*"/>

                <RowDefinition Height="70"/>

            </Grid.RowDefinitions>

            <!-- HEADER -->

            <Border Grid.Row="0"
                    Style="{StaticResource GlassPanel}">

                <Grid MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">

                    <Grid.ColumnDefinitions>

                        <ColumnDefinition Width="*"/>

                        <ColumnDefinition Width="Auto"/>

                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal"
                                VerticalAlignment="Center"
                                Margin="18,0">

                        <Image Source="/Assets/UI/MainDashboardLogo.png"
                               Height="50"
                               Stretch="Uniform"/>

                        <StackPanel Margin="14,0,0,0">

                            <TextBlock Text="SOUNDFX STUDIO"
                                       Foreground="#00D4FF"
                                       FontWeight="Bold"/>

                            <TextBlock Text="Assign Sound To Key"
                                       FontSize="22"
                                       FontWeight="SemiBold"
                                       Foreground="White"/>

                        </StackPanel>

                    </StackPanel>

                    <controls:WindowControlBar Grid.Column="1"
                                                VerticalAlignment="Center"
                                                Margin="0,0,10,0"
                                                ShowMinimize="False"
                                                CloseClicked="CloseButton_Click"/>

                </Grid>

            </Border>

            <!-- SEARCH -->

            <Border Grid.Row="1"
                    Style="{StaticResource GlassPanel}"
                    Margin="0,14,0,14">

                <StackPanel Margin="16">

                    <TextBlock Text="Search Sounds"
                               Foreground="#00D4FF"
                               FontSize="15"
                               FontWeight="SemiBold"
                               Margin="0,0,0,8"/>

                    <TextBox Height="40"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>

                </StackPanel>

            </Border>

            <!-- SOUND LIST -->

            <Border Grid.Row="2"
                    Style="{StaticResource GlassPanel}">

                <Grid>

                    <Grid.RowDefinitions>

                        <RowDefinition Height="*"/>

                        <RowDefinition Height="Auto"/>

                    </Grid.RowDefinitions>

                    <ListBox ItemsSource="{Binding FilteredSounds}"
                             SelectedItem="{Binding SelectedSound, Mode=TwoWay}"
                             Background="Transparent"
                             BorderThickness="0"
                             Foreground="{StaticResource TextBrush}">

                        <ListBox.ItemTemplate>

                            <DataTemplate>

                                <Border Background="#1F13233A"
                                        BorderBrush="#3300D4FF"
                                        BorderThickness="1"
                                        CornerRadius="16"
                                        Margin="0,0,0,10"
                                        Padding="12">

                                    <Grid>

                                        <Grid.ColumnDefinitions>

                                            <ColumnDefinition Width="65"/>

                                            <ColumnDefinition Width="*"/>

                                        </Grid.ColumnDefinitions>

                                        <Border Width="56"
                                                Height="56"
                                                CornerRadius="12"
                                                Background="#0A0F18"
                                                ClipToBounds="True">

                                            <Image Source="{Binding ImagePath}"
                                                   Stretch="UniformToFill"/>

                                        </Border>

                                        <StackPanel Grid.Column="1"
                                                    Margin="12,0,0,0"
                                                    VerticalAlignment="Center">

                                            <TextBlock Text="{Binding Name}"
                                                       FontSize="16"
                                                       FontWeight="SemiBold"
                                                       Foreground="{StaticResource TextBrush}"
                                                       TextTrimming="CharacterEllipsis"/>

                                            <TextBlock Text="{Binding Category}"
                                                       Foreground="{StaticResource MutedBrush}"
                                                       Margin="0,3,0,0"/>

                                            <TextBlock Text="{Binding FilePath}"
                                                       Foreground="#8AA0BC"
                                                       FontSize="11"
                                                       Margin="0,3,0,0"
                                                       TextTrimming="CharacterEllipsis"/>

                                        </StackPanel>

                                    </Grid>

                                </Border>

                            </DataTemplate>

                        </ListBox.ItemTemplate>

                    </ListBox>

                    <TextBlock Grid.Row="1"
                               Text="No sounds match the current search."
                               Foreground="{StaticResource MutedBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,10,0,0"
                               Visibility="{Binding EmptyStateVisibility}"/>

                </Grid>

            </Border>

            <!-- FOOTER -->

            <StackPanel Grid.Row="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center">

                <Button Content="Cancel"
                        Margin="0,0,10,0"
                        Style="{StaticResource GlassButton}"
                        Height="40"
                        Click="CancelButton_Click"/>

                <Button Content="Assign"
                        Style="{StaticResource PrimaryGradientButtonStyle}"
                        Click="AssignButton_Click"/>

            </StackPanel>

        </Grid>

    </Border>

</Window>